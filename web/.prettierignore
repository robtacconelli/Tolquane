node_modules
dist
test-results
playwright-report
e2e/screenshots
../src/tolquane/web/static

# Canvas fixtures, written by `python -m tolquane.web.model parse`: kept byte for byte
# as the model command emits them, so regenerating one is not a formatting diff.
src/test/fixtures/*.json
