"""Tolquane Web: the flow model, run events, storage, scheduler and server behind the GUI.

Install with ``pip install "tolquane[web]"`` and start with ``tolquane web``. The plan is
``docs/web.md``; the contracts between the parts are ``docs/web-interfaces.md``.
"""
